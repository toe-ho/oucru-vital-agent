# Copy to .env — never commit .env
# All host-side ports are here; change to avoid conflicts without touching code.

# ── Host ports ────────────────────────────────────────────────────────────────
BACKEND_PORT=8000
FRONTEND_PORT=3000
POSTGRES_PORT=5432
OLLAMA_PORT=11434

# ── PostgreSQL credentials ─────────────────────────────────────────────────────
POSTGRES_USER=oucru
POSTGRES_PASSWORD=oucru_dev
POSTGRES_DB=oucru_db
